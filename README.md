# Accessible Form UI

A static, accessible form UI built with HTML and CSS — includes proper labeling, ARIA attributes for error states, visible keyboard focus styles, password visibility toggles, and a CSS-only circular progress indicator.

Project: https://roadmap.sh/projects/accessible-form-ui